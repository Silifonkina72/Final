import {StainSliceType} from '../store/slices/StainSlice'
import { BasketState } from '../store/slices/basketSlice';

export type RootState = {
    reducer: {
        stainSlice: StainSliceType[];
        basketSlice: BasketState[];
        logOutSlice:
        regSlice:
        logSlice:
        groundSlice:
        lakSlice:
        primerInsulatorSlice: 
        paintSlice:
        acrylicPrimerSlice:
        patinaSlice:
      },
}