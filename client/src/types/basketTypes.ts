export type Product = {
  id: number;
  name: string;
  priceArea: number;
  model: string;
  priceVolume: number;
  img?: string;
  number: number;
  count?: number;
};

export type Price = {
  allPrice: number;
};
