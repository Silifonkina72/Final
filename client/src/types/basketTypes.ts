export type Product = {
  id: number;
  priceArea: number;
  name: string;
  model: string;
  priceVolume: number;
  img?: string;
  number: number;
  count?: number;
};

export type Price = {
  allPrice: number;
};
